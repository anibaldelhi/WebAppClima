import {createTheme} from '@material-ui/core/styles';

const appTheme = createTheme({
    palette :{
        primary:{
            main : '#0d703a',
        },
        secondary:{
            main: '#79c15d',
        }
    }
});

export default appTheme;